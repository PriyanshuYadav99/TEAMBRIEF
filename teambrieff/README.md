# TeamBrief (Vite + React + Tailwind)

## Quick start

1. Install dependencies:
   ```
   npm install
   ```

2. Run dev server:
   ```
   npm run dev
   ```

3. Build:
   ```
   npm run build
   ```

This project includes Tailwind configuration and React Router.